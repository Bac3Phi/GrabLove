<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_centerInParent="true"
    android:background="@android:color/white"
    tools:context="dev.uit.grablove.Activity.AvatarActivity">

        <ImageButton
            android:id="@+id/btnBackAvatar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_marginLeft="8dp"
            android:adjustViewBounds="true"
            android:maxHeight="40dp"
            android:maxWidth="40dp"
            android:scaleType="fitCenter"
            app:srcCompat="@mipmap/icon_back_40"
            android:background="@android:color/transparent"
            />
        <TextView

            android:fontFamily="sans-serif-smallcaps"
            android:layout_marginTop="60dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="- Choose Your Avatar -"
            android:textSize="30dp"
            android:textColor="@android:color/black"
            android:layout_below="@+id/btnBackAvatar"
             />

        <ImageView
            android:src="@mipmap/icon_gallery"
            android:layout_width="250dp"
            android:layout_height="250dp"
            android:scaleType="fitCenter"
            android:layout_centerInParent="true"
            android:id="@+id/ivAvatar" />

    <at.markushi.ui.CircleButton
        android:id="@+id/btnAddAvatar"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="166dp"
        android:layout_marginEnd="50dp"
        android:src="@drawable/ic_add_black_50dp"
        app:cb_color="#ffffff"
        app:cb_pressedRingWidth="5dp" />

    <info.hoang8f.widget.FButton
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:id="@+id/btnFinishAvatar"
        android:text="@string/finish"
        android:textColor="@android:color/white"
        android:layout_marginBottom="21dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:shadowColor="@android:color/holo_blue_dark"
        app:buttonColor="#a7eaf7"
        app:shadowEnabled="true"
        app:shadowHeight="5dp"
        app:cornerRadius="20dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
